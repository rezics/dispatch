{
  "name": "@rezics/dispatch-cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "src/main.ts",
  "bin": {
    "rezics": "src/main.ts"
  },
  "scripts": {
    "build": "cd ../worker-dashboard && bun run build && cd ../cli && bun build src/main.ts --outdir dist --target bun",
    "start": "bun run src/main.ts start"
  },
  "dependencies": {
    "@rezics/dispatch-worker": "workspace:*",
    "@rezics/dispatch-type": "workspace:*",
    "elysia": "^1.3.0",
    "smol-toml": "^1.3.1",
    "zod": "4.3.6"
  }
}
